<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egovframework.asset.equipment.ReportMapper">
    <insert id="insertReport" parameterType="egovframework.asset.equipment.ReportVO">
        INSERT INTO REPORT (
            equipment_id, user_id, issue_type, content, image_path, rental_id, status, created_at
        ) VALUES (
            #{equipmentId}, #{userId}, #{issueType}, #{content}, #{imagePath}, #{rentalId}, 'PENDING', NOW()
        )
    </insert>

    <!-- 신고 관리 화면(issueList.do)에 보여줄 목록: 아직 종결되지 않은(확인중/고장접수/수리중) 건만 -->
    <select id="selectOpenReports" resultType="map">
        SELECT
            rp.report_id AS reportId,
            u.user_name AS applicant,
            e.equipment_name AS itemName,
            rp.issue_type AS issueType,
            rp.content AS content,
            rp.image_path AS imagePath,
            rp.status AS status
        FROM REPORT rp
        JOIN EQUIPMENT e ON rp.equipment_id = e.equipment_id
        JOIN users u ON rp.user_id = u.user_id
        WHERE rp.status IN ('PENDING', 'CONFIRMED', 'REPAIRING')
        ORDER BY rp.report_id ASC
    </select>

    <select id="selectReportById" parameterType="long" resultType="map">
        SELECT
            report_id AS reportId,
            equipment_id AS equipmentId,
            rental_id AS rentalId,
            status AS status,
            image_path AS imagePath
        FROM REPORT
        WHERE report_id = #{reportId}
    </select>

    <update id="updateReportStatus" parameterType="map">
        UPDATE REPORT
        SET status = #{status}
        WHERE report_id = #{reportId}
    </update>
</mapper>
